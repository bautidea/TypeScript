// We created this file by running in terminal --> tsc 2_first_ts_program.ts

// instead of the 'let' keyword we have 'var', because by default the TS compiler uses
// an older version of JS ES5.
var age = 20;

// In the next lesson we are going to see how to compile TS code to a newer version of JS.
