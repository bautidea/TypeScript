// To use JSDoc we type '/** */'
/**
 * Here we can see that we have one parameter called income, and in {} we can see the
 * type of this parameter
 * @param {number} income
 * @returns {number}
 */
export function calculateTax3(income) {
  return income * 0.3;
}
